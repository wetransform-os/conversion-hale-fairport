# conversion-hale-fairport


Docker image for transforming DFS zone to fAIRport model. The image uses a project created in hale studio to
transform source data (DFS Zone) to fAIRport model in `geojson` format.


